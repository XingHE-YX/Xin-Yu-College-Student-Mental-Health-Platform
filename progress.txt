项目开始。已完成：规范文档（PRD.md, APP_FLOW.md, TECH_STACK.md, FRONTEND_GUIDELINES.md, BACKEND_STRUCTURE.md, IMPLEMENTATION_PLAN.md）和 CODEX.md。
已完成：项目初始化与依赖安装。
已完成：IMPLEMENTATION_PLAN.md 步骤2.1（FastAPI 应用骨架、路由聚合、/health 健康检查接口）。
已完成：IMPLEMENTATION_PLAN.md 步骤2.2（环境变量配置系统、Settings 加载、.env.example、应用启动依赖环境变量）。
当前状态：已完成 IMPLEMENTATION_PLAN.md 步骤1中的项目结构创建、backend 初始化、依赖安装、开发工具配置和小程序基础壳文件创建。
当前状态补充：后端已具备最小可运行入口，GET /health 已通过本地 pytest 验证并返回 200。
当前状态补充：Settings 已接入 app.state，测试环境下可通过环境变量成功启动应用。
已完成：IMPLEMENTATION_PLAN.md 步骤2.3（SQLAlchemy engine、sessionmaker、数据库会话依赖、模型 Base、非测试环境启动连通性检查）。
当前状态补充：FastAPI app.state 已挂载数据库引擎与会话工厂，后续模型与 Alembic 可直接复用同一套元数据和连接配置。
当前状态补充：已通过本地 pytest 验证数据库基础设施相关测试，覆盖 engine 创建、会话依赖、启动检查与健康接口，共 8 项测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤2.4（Alembic 初始化、模型元数据接入、首个空迁移生成与执行验证）。
当前状态补充：已新增 `backend/alembic.ini`、`backend/alembic/env.py`、迁移模板与首个 revision `ad77297002e7_initialize_alembic.py`，Alembic 已连接项目 `Base.metadata`。
当前状态补充：已实际执行 `alembic revision --autogenerate -m "initialize alembic"` 和 `alembic upgrade head`，使用临时 SQLite 数据库验证空迁移链路可生成、可升级。
当前状态补充：已通过本地 pytest 验证 Alembic upgrade/downgrade 与既有数据库基础设施，共 10 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤3.1（`student_users`、`consent_records`、`admin_users` 模型与迁移创建）。
当前状态补充：已新增核心账户枚举与 ORM 模型，覆盖学生账户、不可变授权记录和管理员账户，并按 MySQL 规范落实 `BIGINT UNSIGNED`、`DATETIME(3)`、唯一约束、外键和 `utf8mb4` 表配置。
当前状态补充：已生成 Alembic revision `49e5c01e7c6c_create_core_account_tables.py`，并实际执行 `alembic upgrade head` 验证三张核心账户表可成功迁移。
当前状态补充：已通过本地 pytest 验证核心账户模型编译、约束、默认值以及 Alembic 升级链路，共 12 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤3.2（`questionnaire_templates`、`question_bank`、`questionnaire_submissions`、`questionnaire_answers`、`assessment_reports` 模型与迁移创建）。
当前状态补充：已新增问卷模板、题库、提交记录、答案明细和报告表的 ORM 模型与枚举，并接入学生账户关系；按 MySQL 规范落实 `SMALLINT UNSIGNED`、`BIGINT UNSIGNED`、`DATETIME(3)`、JSON 字段、唯一约束、外键和 `utf8mb4` 表配置。
当前状态补充：已生成 Alembic revision `7570a1e0c36e_create_questionnaire_tables.py`，并实际执行 `alembic upgrade head` 验证五张问卷相关表可成功迁移。
当前状态补充：已通过本地 pytest 验证问卷表 DDL 编译、题库数据导入、答案明细写入、报告持久化以及最新 Alembic head，共 14 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤3.3（`treehole_posts`、`ai_analysis_records`、`post_reactions` 模型与迁移创建）。
当前状态补充：已新增树洞帖子、AI 分析记录和预设互动的 ORM 模型与枚举，并接入学生账户关系；按 MySQL 规范落实 `MEDIUMTEXT`、`BIGINT UNSIGNED`、`INTEGER UNSIGNED`、`DATETIME(3)`、`DECIMAL(5,4)`、JSON 字段、复合唯一约束、外键和 `utf8mb4` 表配置。
当前状态补充：`treehole_posts` 已在数据库层表达 AI 处理状态、发布状态机、软删除字段和公开发布控制；`ai_analysis_records` 已持久化请求快照、原始响应、结构化风险结果与推荐动作；`post_reactions` 已限制为抱抱 / 点亮 / 陪伴三种预设互动并落实唯一约束。
当前状态补充：已生成 Alembic revision `84d79874b9ff_create_treehole_tables.py`，并实际执行 `alembic upgrade head` 验证三张树洞与 AI 相关表可成功迁移。
当前状态补充：已通过本地 pytest 验证树洞表 DDL 编译、默认值、关系持久化、复合唯一约束以及最新 Alembic head，共 16 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤3.4（`alert_cases`、`intervention_logs`、`focus_list_entries`、`audit_logs` 模型与迁移创建）。
当前状态补充：已新增预警工单、干预日志、重点关注和审计日志的 ORM 模型与枚举，并接入学生账户、管理员账户、树洞帖子和问卷提交关系；按 MySQL 规范落实 `BIGINT UNSIGNED`、`DATETIME(3)`、JSON 字段、TEXT 字段、多外键关系和 `utf8mb4` 表配置。
当前状态补充：`alert_cases` 已在数据库层表达来源类型、案例级别、队列状态、复核优先级、复核人和模拟通知日志；`intervention_logs` 已形成与工单和管理员的时间线关系；`focus_list_entries` 已支持需关注来源追踪与生命周期状态；`audit_logs` 已支持对学生 / 管理员 / 系统敏感操作的通用追踪。
当前状态补充：已生成 Alembic revision `5295854b6475_create_review_and_audit_tables.py`，并实际执行 `alembic upgrade head` 验证四张复核与审计相关表可成功迁移。
当前状态补充：已通过本地 pytest 验证复核与审计表 DDL 编译、默认值、关系持久化、外键约束以及最新 Alembic head，共 18 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤4.1（统一题库种子 JSON 结构定义与校验脚本创建）。
当前状态补充：已新增 `backend/src/schemas/question_bank_seed.py`，使用 Pydantic 定义题库种子文件统一结构，覆盖模板元数据、题目选项、分值映射、反向计分和硬触发规则，并校验 `question_id` 前缀、数组顺序、题量一致性、`yes_no` 题型约束和 `score_mapping` 完整性。
当前状态补充：已新增 `backend/src/utils/validate_question_bank_seeds.py` 作为本地校验脚本，可按文件或目录验证 `appendices/question_bank/` 下的题库 JSON；当目录中暂无题库文件时会返回可读提示而非异常中断。
当前状态补充：已新增 `appendices/question_bank/README.md`，明确统一种子文件顶层结构、必填字段、硬触发规则格式、`question_order` 与数组顺序映射关系，以及脚本运行方式，为后续 4.2 / 4.3 录入题库提供固定契约。
当前状态补充：已通过本地 pytest 验证题库种子结构校验、题量一致性、`yes_no` 硬触发约束和目录扫描逻辑，并复跑全部后端测试，共 22 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤4.2（`appendices/question_bank/screen_questions.json`、`sleep_questions.json`、`upi_questions.json` 创建）。
当前状态补充：已补齐三份项目自定义问卷种子文件，分别覆盖 15 题快速筛查、15 题睡眠问卷和 4 题可选 UPI；模板元数据已与冻结需求对齐，其中快速筛查和睡眠问卷为必做并参与完整报告解锁，UPI 为可选且不阻塞解锁。
当前状态补充：快速筛查题目统一采用 1-5 分频率量表，睡眠问卷统一采用 0-3 分频率量表，UPI 统一采用 `yes/no` 选项；三份种子均可直接通过 4.1 中定义的校验结构。
当前状态补充：已按冻结规则写入硬触发定义：`SCREEN_15 >= 4` 对应 `HT-01`，`UPI_01 = yes` 对应 `HT-04`，`UPI_02 = yes` 对应 `HT-05`；睡眠问卷不设置额外硬触发。
当前状态补充：已通过本地校验脚本验证三份真实种子文件，并新增 pytest 覆盖真实文件存在性、题量、评分模式、解锁标记和硬触发规则，复跑全部后端测试后共 23 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤4.3（`appendices/question_bank/sds_questions.json`、`sas_questions.json` 创建）。
当前状态补充：已补齐两份 20 题 Zung 量表种子文件，模板元数据对齐冻结需求，SDS 与 SAS 均设为 `required`、`question_count = 20`、`scoring_mode = zung_standard`、`unlock_required = true`，可供后续评分服务直接复用。
当前状态补充：已按 PRD 固定题号写入反向计分定义：SDS 反向题为 `2、5、6、11、12、14、16、17、18、20`，SAS 反向题为 `5、9、13、17、19`；反向信息已落到每道题的 `reverse_scored` 元数据中。
当前状态补充：已按冻结规则写入硬触发定义：`SDS_15 >= 4` 对应 `HT-02`，`SAS_13 >= 4` 对应 `HT-03`；两份种子均保持 4 级评分选项和完整 `score_mapping`，后续标准分计算可完全依赖题库元数据。
当前状态补充：已通过本地校验脚本验证五份真实题库种子文件，并扩展 pytest 覆盖 SDS / SAS 的题量、评分模式、解锁标记、反向计分题号集合和硬触发配置；复跑全部后端测试后共 23 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤4.4（统一题库导入脚本创建，可插入或刷新 `questionnaire_templates` 与 `question_bank`）。
当前状态补充：已新增 `backend/src/repositories/question_bank_seed_repository.py`、`backend/src/services/question_bank_seed_service.py` 和 `backend/src/utils/import_question_bank_seeds.py`，形成题库文件校验、模板/题目入库、刷新更新和 CLI 执行的完整导入链路。
当前状态补充：导入服务支持按 `template.code` 刷新 `questionnaire_templates`，按 `question_id` 刷新 `question_bank`，自动写入 `question_order` 和 `seed_source`，并在重复导入时更新现有题目、删除已从种子中移除的旧题目，满足“插入或刷新”要求。
当前状态补充：已为题库附录说明补充导入命令文档，并新增 pytest 覆盖空数据库初始化、重复导入刷新与 CLI `--database-url` 覆盖场景；复跑全部后端测试后共 26 项后端测试通过。
当前状态补充：已实际执行 `PYTHONPATH=backend backend/.venv/bin/python -m src.utils.import_question_bank_seeds --database-url sqlite+pysqlite:////tmp/xinyu_question_bank_import.db appendices/question_bank`，在临时 SQLite 数据库中成功导入 5 个问卷模板和 74 条题库记录。
已完成：IMPLEMENTATION_PLAN.md 步骤5.1（快速筛查、SDS、SAS、睡眠问卷、UPI 评分服务实现）。
当前状态补充：已新增 `backend/src/services/questionnaire_scoring_service.py`，实现五类问卷评分器、统一评分结果结构、逐题标准化得分产物和快照输出；服务层可直接复用题库种子或后续 `question_bank` ORM 元数据进行评分。
当前状态补充：快速筛查已按总分 `< 45 / >= 45` 输出低风险与需关注；睡眠问卷已按 `0-7 / 8-14 / >= 15` 输出低风险、需关注、高风险；SDS 与 SAS 已按 Zung 标准分 `int(1.25 * raw_score)` 计算标准分，并分别按 `53 / 63` 与 `50 / 60` 边界输出风险等级；UPI 在 5.1 阶段保持辅助评分，不在此步直接升级为高风险。
当前状态补充：评分服务已支持漏答校验、意外题号校验、非法选项校验，以及 SDS / SAS 反向计分；`reverse_scored` 元数据已在评分时生效，后续 5.2 可直接在现有逐题结果基础上叠加硬触发判断。
当前状态补充：已新增 pytest 覆盖五类问卷的阈值边界、Zung 反向计分和漏答错误场景，并复跑全部后端测试后共 33 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤5.2（硬触发判断逻辑实现）。
当前状态补充：评分服务现已基于题库元数据执行硬触发判断，覆盖 `SCREEN_15 >= 4`、`SDS_15 >= 4`、`SAS_13 >= 4`、`UPI_01 = yes`、`UPI_02 = yes` 五条冻结规则；命中后会将原本按总分得到的结果直接提升为高风险。
当前状态补充：已为评分结果新增 `hard_trigger_hit` 与 `hard_trigger_matches` 结构，记录命中题号、规则编码、比较运算符和实际命中值；`to_snapshot()` 现会输出硬触发信息，可直接供后续 `questionnaire_submissions.hard_trigger_hit` 和风险闭环逻辑复用。
当前状态补充：硬触发比较统一使用题目原始作答值 / 原始分值进行判断，而不是反向计分后的标准化分值；因此 `SAS_13` 即使是反向计分题，也会按用户原始选择 `>= 4` 正确升级为高风险。
当前状态补充：已新增 pytest 覆盖五条硬触发规则、无触发时的边界分层、UPI 从辅助结果升级为高风险、以及快照输出中的 `hard_trigger_hit` 字段；复跑全部后端测试后共 38 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤5.3（单量表结果、报告摘要状态与完整综合画像报告生成逻辑实现）。
当前状态补充：已新增 `backend/src/services/assessment_report_service.py`，统一实现单量表报告生成、`S10` 报告摘要状态构建和 `S10A` 完整综合画像报告生成；输出结构已包含 `hero-card`、`result-badge`、`progress-bar`、`safety-banner` 与按钮变体提示，可直接供后续小程序报告页渲染使用，并遵循前端设计规范中的平静、可信、低压基调。
当前状态补充：报告服务现已强制执行完整报告解锁规则，仅当快速筛查、SDS、SAS、睡眠问卷四份必做问卷全部完成时才允许生成 `full_profile` 报告；进度按固定 70 题链路计算，缺失问卷会在摘要状态中返回缺口列表与下一步动作；可选 UPI 不阻塞解锁，但若命中高风险硬触发，仍可升级完整报告风险等级。
当前状态补充：完整报告综合风险聚合已按 PRD 的完整数据规则实现，支持基于最新一次提交生成各量表摘要、综合结论、趋势占位区、调节建议和免责声明；报告内容会优先采用每份问卷的最新提交，避免旧结果覆盖新结果。
当前状态补充：已新增 pytest 覆盖单量表报告结构、70 题锁定 / 解锁、缺失必做问卷时报错、最新提交覆盖旧提交、睡眠高分在完整画像中维持 `watch` 聚合、以及可选 UPI 在不阻塞解锁的前提下升级综合风险等场景；复跑全部后端测试后共 43 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤6.1（学生微信登录 API 与演示登录 API 实现）。
当前状态补充：已新增 `backend/src/core/security.py`，使用标准库实现 HS256 风格访问令牌签发与校验；`create_app()` 已将访问令牌服务与微信会话交换服务挂载到 `app.state`，供后续学生端认证依赖和管理员认证复用。
当前状态补充：已新增 `backend/src/services/wechat_session_service.py`、`backend/src/services/student_auth_service.py`、`backend/src/repositories/student_user_repository.py`、`backend/src/schemas/student_auth.py` 与路由 `backend/src/api/routes/student_auth.py`，打通 `POST /api/v1/auth/student/wechat-login` 与 `POST /api/v1/auth/student/demo-login`；微信登录会调用 `jscode2session` 交换 openid，按手机号与 openid 创建或刷新 `student_users`，并返回学生访问令牌与基础资料。
当前状态补充：为兼容 `APP_FLOW.md` 中“登录成功后若缺少授权记录则进入 S03 授权页”的主流程，登录接口当前只返回 `student.consent_status` 供前端判断，不在 6.1 阶段持久化授权决定；请求中的 `consent_status` 被保留为兼容字段，真正的授权写入将留到 6.2 的 `/api/v1/consents` 实现。
当前状态补充：演示登录现已严格受 `ENABLE_DEMO_LOGIN` 控制；开启时会创建或复用固定 demo 学生账号并返回 JWT，关闭时返回业务错误 `DEMO_LOGIN_DISABLED`；登录服务同时覆盖手机号 / openid 冲突保护，避免两个学生账户被错误合并。
当前状态补充：已新增 pytest 覆盖微信登录新建账号、返回用户刷新、手机号与 openid 冲突、演示登录开关、访问令牌 claims 校验等场景；复跑全部后端测试后共 48 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤6.2（授权提交接口、授权记录持久化与学生授权状态同步实现）。
当前状态补充：已新增 `backend/src/core/auth.py`，实现学生 Bearer 令牌解析、JWT 校验和当前学生回查；受保护接口现在会按 `student_id` 从数据库读取最新学生状态，而不是直接信任令牌中的 `consent_status`，避免授权状态变更后出现旧令牌信息滞后。
当前状态补充：已新增 `backend/src/schemas/consent.py`、`backend/src/repositories/consent_record_repository.py`、`backend/src/services/student_consent_service.py` 与路由 `backend/src/api/routes/consents.py`，打通 `POST /api/v1/consents`；接口支持提交 `privacy_policy` 与 `crisis_intervention_authorization` 两类授权记录，并以追加写入方式保存到 `consent_records`，保证授权决策可追溯。
当前状态补充：当提交的是 `crisis_intervention_authorization` 时，服务层会同步更新 `student_users.consent_status` 为 `granted` 或 `declined`；提交成功后接口会返回最新学生资料、刚写入的 `consent_record` 以及刷新后的学生访问令牌，便于前端在 `S03` 成功后立即更新本地会话状态并按流程进入 `S04 首页`。
当前状态补充：隐私授权记录不会错误覆盖危机干预授权状态；重复提交危机授权会继续追加不可变历史记录，同时以最新一次决策作为 `student_users.consent_status` 的当前值，满足“可追溯 + 当前态同步”两个目标。
当前状态补充：已新增 pytest 覆盖危机授权同意、危机授权拒绝、重复提交形成历史、隐私授权不影响危机授权状态、未登录拒绝访问等场景；复跑全部后端测试后共 52 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤6.3（学生端启动页、登录页、授权页与首页主流程实现）。
当前状态补充：已在 `miniprogram/` 下新增 `pages/startup/`、`pages/login/`、`pages/consent/`、`pages/home/` 四个页面，并更新 `app.json` 路由顺序；`S01` 现会检查本地会话与授权状态，自动在登录页、授权页和首页之间跳转，满足 `APP_FLOW.md` 中启动入口与授权分流逻辑。
当前状态补充：已新增 `miniprogram/constants/`、`services/`、`utils/` 与 `styles/` 下的共享前端基础设施，包括 API 配置、会话存储、访问令牌本地解码、请求封装与认证服务调用；当前小程序端已可直接调用 `POST /api/v1/auth/student/wechat-login`、`POST /api/v1/auth/student/demo-login` 与 `POST /api/v1/consents`。
当前状态补充：登录页已实现微信登录、失败重试、手机号授权拒绝阻断提示与演示登录入口；考虑到后端当前登录接口尚未对接真实微信手机号解密，本阶段采用“`wx.login` + 受控手机号表单”作为开发联调桥接方案，以保证 6.1 / 6.2 / 6.3 链路可在本地完整跑通，同时清晰提示该手机号授权为开发联调模式。
当前状态补充：授权页已实现隐私说明、危机干预授权说明、同意 / 拒绝两条路径以及拒绝确认弹窗；无论同意还是拒绝，前端都会调用 `/api/v1/consents` 记录授权结果并刷新本地访问令牌，随后进入首页；首页已根据 `consent_status` 正确展示“树洞入口禁用”状态，并为报告锁定状态展示固定 `0 / 70` 进度占位。
当前状态补充：已按 `FRONTEND_GUIDELINES.md` 新增并落盘学生端业务组件 `app-shell`、`hero-card`、`primary-button`、`secondary-button`、`danger-button`、`question-card`、`progress-bar`、`result-badge`、`safety-banner`、`treehole-post-card`、`reaction-chip`、`confirm-modal`，为后续 7.x / 8.x 页面复用统一视觉和交互骨架。
当前状态补充：本地已完成小程序 `json` 文件结构校验，`miniprogram/` 下共 18 个 `json` 文件可成功解析；当前环境缺少 `node` / `nodejs` 与微信开发者工具，因此尚未执行小程序运行态自动校验，已通过人工检查关键 `js` 入口与页面流转逻辑完成本步最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤7.1（问卷列表接口、问卷详情接口、必做进度接口与学生端首页元数据接入）。
当前状态补充：已新增 `backend/src/services/questionnaire_query_service.py`、`backend/src/schemas/questionnaire.py` 与路由 `backend/src/api/routes/questionnaires.py`，打通 `GET /api/v1/questionnaires`、`GET /api/v1/questionnaires/{code}` 和 `GET /api/v1/questionnaires/progress`；接口现基于已冻结的题库种子文件输出问卷目录、可渲染题目结构和固定 70 题必做进度，同时叠加当前学生最新一次提交摘要，直接服务后续 7.2 / 7.3 / 7.4。
当前状态补充：问卷详情接口当前只返回前端渲染所需元数据（题目文案、题型、选项、问卷基础信息），不会向学生端暴露 `score_mapping`、反向计分或硬触发规则等评分内部细节；问卷列表与进度接口按 `APP_FLOW.md` 固定顺序返回 `SCREEN`、`SDS`、`SAS`、`SLEEP`、`UPI`，并正确区分必做 / 可选与 `S05 - S09` 流程阶段。
当前状态补充：已新增 `tests/backend/test_questionnaire_api.py`，覆盖问卷列表顺序、问卷详情结构、必做进度去重统计、最新提交摘要透出、未知问卷 404 和未登录拒绝访问等场景；复跑全部后端测试后共 58 项后端测试通过。
当前状态补充：学生端已新增 `miniprogram/services/questionnaires.js`，首页 `pages/home/` 已从固定占位改为真实请求问卷目录与必做进度，并按 `FRONTEND_GUIDELINES.md` 的平静、可信、低压基调重构“问卷中心”和报告进度卡片；当前首页可展示真实的 70 题完成度、待完成问卷列表、各问卷必做 / 可选状态以及最近一次风险结果标签，同时保留树洞 / 报告 / 帮助资源入口。
当前状态补充：本地再次完成小程序 `json` 文件结构校验，`miniprogram/` 下共 18 个 `json` 文件可成功解析；校验过程中 `python3` 因沙箱环境对 `/tmp` 写入受限出现 `xcrun` 相关警告，但不影响 JSON 解析结果。当前环境仍缺少 `node` / `nodejs` 与微信开发者工具，因此尚未执行小程序运行态自动校验，已结合后端接口测试和前端代码人工检查完成本步最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤7.2（问卷提交接口、评分执行、提交记录与答案明细持久化实现）。
当前状态补充：已在 `backend/src/api/routes/questionnaires.py` 中打通 `POST /api/v1/questionnaires/{code}/submissions`，并在 `backend/src/schemas/questionnaire.py` 中新增问卷提交请求 / 响应结构；接口当前按 `BACKEND_STRUCTURE.md` 合约接收 `answers[{question_code, selected_option}]`，返回 `submission_id`、`questionnaire_code`、`raw_score`、`standardized_score`、`risk_level` 和 `hard_trigger_hit`，可直接供后续 7.3 页面提交后展示结果摘要。
当前状态补充：已新增 `backend/src/services/questionnaire_submission_service.py`，问卷提交链路现会先根据已冻结题库种子确认问卷编码存在，再校验数据库中的 `questionnaire_templates` / `question_bank` 元数据是否已通过 4.4 导入且与种子保持一致；通过后使用 `QuestionnaireScoringService` 执行漏答校验、非法选项校验、反向计分、标准分换算和硬触发升级，并将结果写入 `questionnaire_submissions` 与 `questionnaire_answers`。
当前状态补充：`questionnaire_submissions` 当前会在服务端同步写入 `status = scored`、`raw_score`、`standardized_score`、`risk_level`、`hard_trigger_hit` 和完整 `scoring_snapshot_json`；`questionnaire_answers` 会逐题保存 `question_id`、`selected_option`、`raw_value` 与 `normalized_score`，确保后续报告生成、预警闭环和审计链路可直接复用本次提交产物。
当前状态补充：提交接口已补齐错误分支：漏答时返回 `QUESTIONNAIRE_SUBMISSION_INCOMPLETE` 并带结构化 `errors` 列表，非法或重复作答返回 `QUESTIONNAIRE_ANSWER_INVALID`，未知问卷返回 `QUESTIONNAIRE_NOT_FOUND`，数据库未导入题库或元数据与种子失配时返回 `QUESTIONNAIRE_CATALOG_UNAVAILABLE`；因此前端在 7.3 可同时处理本地漏答拦截和服务端兜底校验。
当前状态补充：学生端 `miniprogram/services/questionnaires.js` 已补充 `submitQuestionnaire()` 请求封装，为后续 `S05 - S09` 问卷页面直接复用；该改动不提前实现 7.3 页面，但已把提交 API 的调用入口固定下来。
当前状态补充：已新增 `tests/backend/test_questionnaire_submission_api.py`，覆盖快速筛查持久化、SDS / SAS 标准分返回、睡眠问卷与 UPI 成功提交、UPI 硬触发升级高风险、漏答拒绝、非法选项拒绝、未导入题库时的 503、未登录拒绝访问、未知问卷 404 以及答案明细外键关联等场景；复跑全部后端测试后共 68 项后端测试通过。
当前状态补充：已额外执行 `ruff check` 校验本次新增与改动的 Python 文件，当前静态检查通过。由于本步只新增后端接口与小程序服务封装，未修改小程序 `json` 配置或页面结构，因此未重复执行微信小程序运行态检查。
已完成：IMPLEMENTATION_PLAN.md 步骤7.3（快速筛查、SDS、SAS、睡眠问卷、UPI 五个小程序问卷页面实现）。
当前状态补充：已新增 `miniprogram/pages/questionnaires/` 下的五个页面壳文件，并通过 `miniprogram/utils/questionnaire-page.js` 抽出共享问卷页运行器；当前 `S05 - S09` 会按问卷编码复用同一套加载、作答、提交、结果展示和导航逻辑，同时保留各页面独立路由、独立导航栏标题和分阶段文案，满足 `APP_FLOW.md` 中五条学生端问卷流程。
当前状态补充：已新增 `miniprogram/constants/questionnaires.js` 和共享模板 `miniprogram/pages/questionnaires/shared.wxml` / `miniprogram/styles/questionnaire-page.wxss`，统一定义每份问卷的标题、说明、分数标签、辅助提示和提交流转文案；页面视觉严格沿用 `hero-card`、`question-card`、`progress-bar`、`result-badge`、`safety-banner`、`primary-button`、`secondary-button`、`danger-button` 等既有组件，整体保持平静、可信、低压基调，并在高风险结果下提升安全提示层级。
当前状态补充：五个问卷页现已支持从首页真实进入；首页 `pages/home/` 已从“点击后 toast 占位”改为按问卷编码跳转到对应页面。问卷页加载时会请求 `GET /api/v1/questionnaires/{code}` 与 `GET /api/v1/questionnaires/progress`，展示当前问卷元数据、70 题固定进度或 UPI 可选说明，以及“此前已作答，重新提交以最新结果为准”的温和提示。
当前状态补充：前端现已实现本地漏答拦截：未完成全部题目时会阻止提交、给缺失题目外层加红色提醒包裹、滚动到首个未答题，并保留已填答案不丢失；若后端仍返回 `QUESTIONNAIRE_SUBMISSION_INCOMPLETE`，前端也会根据结构化 `errors` 回显缺失题目。服务端错误透传已通过 `miniprogram/services/request.js` 补齐 `errors` 字段，为 7.3 页面和后续表单页复用提供统一约定。
当前状态补充：问卷提交成功后，页面会切换到结果态并展示风险标签、分数摘要、免责声明、固定进度刷新结果和下一步动作；`SCREEN -> SDS -> SAS -> SLEEP` 已按流程提供“继续下一份问卷”跳转，`SLEEP` 与 `UPI` 完成后返回首页，`UPI` 明确标注“仅作辅助参考、不影响完整报告解锁”；高风险结果会额外展示 `safety-banner` 与热线电话，并将主按钮切换为危险色，符合前端规范中的高风险表达要求。
当前状态补充：本地再次完成小程序 `json` 文件结构校验，`miniprogram/` 下共 23 个 `json` 文件可成功解析；同时复跑全部后端测试后仍为 68 项通过。当前环境依旧缺少 `node` / `nodejs` 与微信开发者工具，因此尚未执行小程序运行态自动校验，已通过页面结构人工检查、JSON 校验和后端接口回归测试完成本步最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤7.4（报告页、完整报告页与报告接口实现）。
当前状态补充：已新增 `backend/src/api/routes/reports.py`、`backend/src/schemas/report.py` 与 `backend/src/services/student_report_service.py`，打通 `GET /api/v1/reports/summary`、`GET /api/v1/reports/full`、`GET /api/v1/reports/history`；接口当前基于最新 `questionnaire_submissions` 与 5.3 已实现的 `AssessmentReportService` 现算生成报告摘要、完整综合画像和测评历史记录，不依赖额外手工同步步骤。
当前状态补充：报告摘要接口现会严格输出 `S10` 三种状态：`locked`、`partial`、`unlocked`。当尚未完成 70 道必做题时，会返回已完成量表摘要、缺失问卷、进度条数据和下一步行动按钮；当 70 题完成后，会解锁 `S10A` 完整报告入口。`GET /api/v1/reports/full` 在未解锁时返回业务错误 `FULL_PROFILE_LOCKED`，前端据此回退到报告摘要页，保证锁定 / 解锁逻辑与需求严格一致。
当前状态补充：学生端已新增 `miniprogram/services/reports.js`、`miniprogram/pages/reports/index.*`、`miniprogram/pages/reports/full/index.*` 与共享样式 `miniprogram/styles/report-page.wxss`；`S10 报告页` 当前会展示完整报告解锁进度、已完成量表结果、最近测评历史、下一步动作和免责声明，`S10A 完整报告详情页` 当前会展示综合结果、各量表摘要、趋势占位区、调节建议和安全提示，整体视觉延续 `FRONTEND_GUIDELINES.md` 要求的平静、可信、低压表达。
当前状态补充：首页报告模块现已从占位提示切换为真实跳转到 `S10`；报告摘要页中的 CTA 会按 `APP_FLOW.md` 分流到未完成问卷或完整报告详情页。为增强 7.3 与 7.4 的联动，问卷页在完成最终必做问卷并达到解锁条件后，主按钮也会直接切换为“查看完整报告”。
当前状态补充：报告页会在高风险状态下渲染 `safety-banner` 并注入热线电话，确保高风险表达层级高于普通信息；帮助资源按钮目前仍保留为后续步骤入口提示，不提前虚构 `S15` 页面。测评历史区域现已通过 `GET /api/v1/reports/history` 展示按时间倒序排列的历史记录，满足 PRD 中“测评历史记录”能力的最小闭环。
当前状态补充：已新增 `tests/backend/test_report_api.py`，覆盖无提交时的 `locked` 摘要、部分完成时的 `partial` 摘要与历史记录、完整报告未解锁时的 409、70 题完成后的 `full_profile` 返回以及未登录拒绝访问等场景；复跑全部后端测试后共 73 项通过，并额外执行 `ruff check` 校验新增后端文件，当前静态检查通过。
当前状态补充：本地再次完成小程序 `json` 文件结构校验，`miniprogram/` 下共 25 个 `json` 文件可成功解析。当前环境仍缺少 `node` / `nodejs` 与微信开发者工具，因此尚未执行小程序运行态自动校验，已通过 JSON 校验、页面结构人工检查和后端回归测试完成本步最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤8.1（树洞数据接口实现）。
当前状态补充：已新增 `backend/src/api/routes/treehole.py`、`backend/src/schemas/treehole.py`、`backend/src/services/treehole_service.py` 与 `backend/src/repositories/treehole_repository.py`，打通 `GET /api/v1/treehole/feed`、`POST /api/v1/treehole/posts`、`DELETE /api/v1/treehole/posts/{post_id}` 和 `POST /api/v1/treehole/posts/{post_id}/reactions`；已发布内容现可在广场被读取、互动和删除。
当前状态补充：广场接口当前只返回公开所需字段（匿名昵称、匿名头像标识、脱敏正文、发布时间、互动汇总以及“我是否已互动”状态），响应结构可直接服务后续 `treehole-post-card` 与 `reaction-chip` 组件渲染；接口严格不返回 `content_raw`，并只暴露 `publish_status = published` 且 `allow_publication = true` 的帖子，确保被拦截 / 已删除内容不会出现在学生端公开广场。
当前状态补充：发帖接口当前按第 8 阶段 bootstrap 路径实现基础脱敏与占位分析记录：仅 `consent_status = granted` 的学生可发帖，空内容会被拒绝，手机号与邮箱会在公开正文中被替换；服务端会为新帖写入 `ai_analysis_records` 占位记录，并以 `ai_status = mocked`、`risk_level = low`、`publish_status = published` 先打通低风险可发布链路，为 9.1 - 9.4 的正式 AI 集成与风险聚合保留清晰替换点。
当前状态补充：删除接口已采用软删除语义：学生删除后会把帖子状态写为 `deleted_by_user`、关闭 `allow_publication` 并写入 `deleted_at`，数据库记录保留不丢失；互动接口支持 `hug / light / accompany` 三种预设反应，同一学生对同一帖子同一种 reaction 重复提交会按幂等处理，不会重复计数。
当前状态补充：已新增 `tests/backend/test_treehole_api.py`，覆盖广场公开过滤、发帖持久化与脱敏、授权拒绝拦截、软删除后广场隐藏、互动幂等和非公开帖子拒绝互动等场景；已执行 `ruff check --no-cache` 校验新增文件，并复跑全部后端测试后共 79 项后端测试通过。
已完成：IMPLEMENTATION_PLAN.md 步骤8.2（小程序树洞页面实现）。
当前状态补充：已新增 `miniprogram/pages/treehole/feed/`、`miniprogram/pages/treehole/create/`、`miniprogram/pages/treehole/detail/` 三个页面，并更新 `miniprogram/app.json` 与 `miniprogram/constants/config.js` 路由常量；首页 `pages/home/` 的树洞模块现已从“后续接入”占位切换为真实跳转，可按 `APP_FLOW.md` 进入 `S11 树洞广场页`。
当前状态补充：已新增 `miniprogram/services/treehole.js` 与 `miniprogram/utils/treehole.js`，封装 `feed / create / delete / reactions` 四个树洞接口请求，以及帖子缓存、发布时间格式化、reaction 合并与详情路由构建逻辑；`S11` 当前会真实加载广场内容并支持预设互动，互动失败时会回滚前端状态，符合流程文档中的错误处理要求。
当前状态补充：`S12` 树洞发布页现已支持文本输入、前端空内容校验、低风险发布成功状态和高风险拦截态占位；当前因 8.1 后端仍处于 bootstrap 发帖路径，实际联调结果会稳定落入“发布成功”分支，从而打通“广场 -> 发布 -> 发布成功 -> 查看我的帖子”的低风险闭环。发布成功后可直接返回广场或进入帖子详情页。
当前状态补充：`S16` 我的帖子详情页现已支持展示完整正文、发布时间、支持反馈汇总与删除确认弹窗；删除会调用 `DELETE /api/v1/treehole/posts/{post_id}`，成功后返回广场并触发重新加载，因此该帖子会从学生端列表中消失，为 8.3 的软删除规则前端表现打下基础。
当前状态补充：已扩展 `miniprogram/components/treehole-post-card/`，补齐匿名头像、相对时间、6 行正文截断与头部插槽能力，并新增共享样式 `miniprogram/styles/treehole-page.wxss`；整体视觉沿用 `FRONTEND_GUIDELINES.md` 的品牌色、留白、卡片圆角与低压表达，不引入第三方组件库。
当前状态补充：本地已完成小程序 `json` 文件结构校验，`miniprogram/` 下共 28 个 `json` 文件可成功解析，并额外校验了 `app.json` 中所有页面路由都能映射到实际的 `.js/.wxml/.wxss/.json` 文件，以及新增树洞页面中的相对 `require(...)` 路径均存在。当前环境仍缺少 `node` / `nodejs` 与微信开发者工具，因此尚未执行小程序 JS 语法自动检查或运行态验证；已通过路径校验和代码人工检查完成本步最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤8.3（树洞软删除规则前端表现补齐）。
当前状态补充：学生在 `S16` 删除帖子后，详情页会先切换为“已从学生端移除”状态并写入本地 tombstone 缓存，再返回 `S11` 广场；广场页会消费一次性成功提示并重新拉取 `feed`，因此该帖子会从学生端列表与详情入口中消失，同时保留“后台仍保留记录”的明确文案，符合 `APP_FLOW.md` 与 PRD 删除规则。
当前状态补充：`miniprogram/utils/treehole.js` 已补充 `deleted_at` 标准化、`deleted_by_user` 展示标签、本地软删除快照构建和一次性删除提示存储；若用户再次进入已删除帖子的旧详情路由，页面会显示软删除状态而不再展示正文或删除按钮，避免学生端继续暴露已移除内容，同时不影响后台后续查询软删除记录。
当前状态补充：已执行 `git diff --check` 验证本次受影响的小程序文件无基础格式问题。当前环境仍缺少 `node` / `nodejs` 与微信开发者工具，因此未执行小程序 JS 语法自动检查或运行态验证；本步通过代码人工检查与状态流转回读完成最小验证。
已完成：IMPLEMENTATION_PLAN.md 步骤9.1（DeepSeek 客户端实现）。
当前状态补充：已新增 `backend/src/services/deepseek_service.py`，使用 `httpx` 直连 `https://api.deepseek.com/chat/completions`，固定模型为 `deepseek-chat`，并通过 `response_format = {"type": "json_object"}` 与系统提示词双重约束模型只返回 JSON；客户端当前会返回标准化的 `request_payload`、原始响应体、首个 choice 的 `content_text` 以及解析后的 `content_json`，为后续 9.2 回退路径和 9.3 风险聚合直接复用。
当前状态补充：`backend/src/main.py` 现已在应用启动时实例化并挂载 `app.state.deepseek_service`，与既有 `access_token_service`、`wechat_session_service` 保持同一注入模式；当前树洞发布仍维持 8.x 的 bootstrap 占位链路，不提前切换到正式 AI 审核，以避免把 9.2 模拟回退和 9.3 风险聚合职责混入本步。
当前状态补充：已新增 `tests/backend/test_deepseek_service.py`，覆盖正常 JSON 返回、非 2xx 响应、非 JSON 文本和非对象 JSON 四类场景；并扩展 `tests/backend/test_database.py` 校验 `create_app()` 已暴露 `deepseek_service`。本地已执行 `ruff check --no-cache` 校验受影响 Python 文件，随后复跑全部后端测试，当前共 83 项后端测试通过。
当前状态补充：本步未修改小程序页面、样式或路由，因此无需新增前端视觉实现；`FRONTEND_GUIDELINES.md` 与 `APP_FLOW.md` 已继续作为约束参考，其中 `S12` 中的“AI 调用失败 -> 走模拟回退路径”仍待 9.2 正式接入。
已完成：IMPLEMENTATION_PLAN.md 步骤9.2（DeepSeek 模拟回退路径实现）。
当前状态补充：已新增 `backend/mock_response.json` 作为本地 AI 回退结果文件，并扩展 `backend/src/services/deepseek_service.py`：当前除常规 `create_json_completion()` 外，已新增 `create_json_completion_with_fallback()`，会在 DeepSeek 超时、非 2xx 响应或返回非法 JSON 时自动读取本地 `mock_response.json`，返回统一结构的 `content_json`、`response_payload`、`fallback_used` 与 `fallback_reason`，满足 `TECH_STACK.md` 中“优先调用 DeepSeek，失败时回退模拟数据”的要求。
当前状态补充：树洞发帖链路现已真正接入 AI 回退包装层：`backend/src/api/routes/treehole.py` 会把 `app.state.deepseek_service` 注入 `TreeholeService`，`backend/src/services/treehole_service.py` 在创建帖子时会先保存原始文本与脱敏正文，再调用 DeepSeek 或本地 mock 路径生成结构化分析结果，并将 `request_payload_json`、`response_raw_json`、`parsed_risk_level`、`parsed_risk_score`、`emotion_tags_json`、`trigger_phrases_json`、`reason_text`、`recommended_action` 和 `fallback_used` 持久化到 `ai_analysis_records`；当回退命中时，帖子 `ai_status` 会写为 `mocked`，从而保证 AI 异常时树洞主流程不中断。
当前状态补充：为避免提前混入 9.3 / 9.4 的风险聚合与发布决策，本步仍保留现阶段树洞的低风险公开主流程：即使远端 DeepSeek 已返回结构化风险结果，`treehole_posts.publish_status` 与学生端 `S12` 返回分支仍维持当前 bootstrap 低风险发布行为；真正的 `watch / high` 决策切换、重点关注与安全拦截仍待后续步骤实现。
当前状态补充：已新增 / 扩展 `tests/backend/test_deepseek_service.py` 与 `tests/backend/test_treehole_api.py`，覆盖超时回退、非 200 回退、非法 JSON 回退、错误 mock 文件、树洞发帖在 mock 回退下成功落库，以及远端 AI 成功时分析记录持久化等场景；本地已执行 `ruff check --no-cache` 校验受影响 Python 文件，并复跑全部后端测试，当前共 88 项后端测试通过。
当前状态补充：本步未修改小程序页面、样式或路由；`FRONTEND_GUIDELINES.md` 与 `APP_FLOW.md` 已继续作为约束参考，其中 `S12` 的前端页面结构无需变更即可承接后续 9.3 / 9.4 的正式风险分流结果。
已完成：IMPLEMENTATION_PLAN.md 步骤9.3（风险聚合器实现）。
当前状态补充：已新增 `backend/src/services/risk_aggregation_service.py`，实现 `RiskAggregationService` 与 `AggregatedRiskResult`；当前聚合器会统一汇总 AI 文本风险、最新问卷结果、量表硬触发和历史高风险状态，并输出稳定的 `low / watch / high` 结论、`reason_codes`、`history_elevated`、`hard_trigger_hit`、`required_chain_complete` 与 `latest_questionnaire_codes` 等结构化字段，便于后续 9.4 直接接入树洞发布决策。
当前状态补充：聚合规则已按 PRD 与 `BACKEND_STRUCTURE.md` 固化：任意问卷硬触发、`AI high`、`SDS high` 或 `SAS high` 直接判定为高风险；`AI watch`、`SDS/SAS 阳性` 判定为需关注；睡眠风险仅在 `SCREEN + SDS + SAS + SLEEP` 四份必做问卷齐全时才会把综合结果提升为需关注，以避免不完整数据场景被单一睡眠量表过度放大；若用户存在历史高风险但当前 AI 与最新问卷信号均为中性，则聚合器会把本次结果提升为 `watch` 并标记 `HISTORY_HIGH_REVIEW`。
当前状态补充：聚合器同时支持数据库上下文加载：`aggregate_treehole_risk(student=..., ai_risk_level=...)` 会自动读取该学生每份问卷的最新提交，并从 `student_users.risk_status`、历史高风险问卷提交以及历史树洞 `ai_analysis_records.parsed_risk_level = high` 中识别“历史高风险状态”；因此即使当前账户 `risk_status` 仍未在前序步骤中主动回写，高风险历史也能在 9.3 阶段被正确识别并用于复查提升。
当前状态补充：为避免提前把 9.4 的发布决策逻辑混入本步，当前树洞 `POST /api/v1/treehole/posts` 仍保持 9.2 阶段的 bootstrap 返回分支，不在本步直接修改学生端 `S12` 的拦截 / 发布响应；这样可以避免出现“前端已按高风险拦截，但后端仍未切换 `publish_status`”的状态错位。9.4 将基于本步聚合器结果统一接入 `published / focus list / blocked_high_risk` 决策。
当前状态补充：已新增 `tests/backend/test_risk_aggregation_service.py`，覆盖只有树洞时 `AI watch -> watch`、完整必做链路下睡眠高分映射为 `watch`、不完整链路下睡眠单独不提升、问卷硬触发直接判高、历史高风险 + 当前中性提升为 `watch`，以及数据库上下文下“最新提交选择”与“历史高风险来源查询”两类场景；本地已执行 `ruff check --no-cache` 校验新增 / 改动 Python 文件，并复跑全部后端测试，当前共 95 项后端测试通过。
当前状态补充：本步未修改小程序页面、样式或路由；`FRONTEND_GUIDELINES.md` 与 `APP_FLOW.md` 已继续作为约束参考，其中树洞发布页 `S12` 的前端状态分流将在 9.4 接入正式发布决策后再切换到真实高风险拦截流。
待办：见 IMPLEMENTATION_PLAN.md 步骤9.4。
